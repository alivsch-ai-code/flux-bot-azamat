# Imports nach sys.path / load_dotenv — absichtlich nicht ganz oben
[lint.per-file-ignores]
"main.py" = ["E402"]
"src/application/services.py" = ["E402"]
"archive/legacy_tools/main_gui.py" = ["E402"]
