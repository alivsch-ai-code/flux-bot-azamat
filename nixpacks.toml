[phases.setup]
nixPkgs = ["nodejs_24", "python312", "gcc"]

[phases.install]
cmds = [
  "npm ci --prefix webapp-react",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "npm run build --prefix webapp-react"
]

[start]
cmd = "python main.py"

